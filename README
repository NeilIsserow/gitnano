## WARNING — Use at your own risk  
**NB! This code is provided as‑is, with no guarantee it will work or not do any damage.  
No warranty or support is provided and this is developed independently of my
employer with the help of AI. Use of this repo is at your own risk!**

---
Use gitnano.sh if you are standing up a brand new host from scratch or need to trigger a full seed migration from your upstream source.
Use gitnano2.sh if the application is already running, but your Puppet agent runs are hanging or throwing an SSH connection error when trying to fetch data from the local repository.
Use gitnano.pp with common.yaml in your PE setup once gitnano is up and running to ensure it stays up and running on every Puppet Agent run.
---
# 🚀 GitNano: Unified Control-Repo Manager

GitNano is a lightweight Python-based bridge between your Puppet Master and your
Git repositories, designed for single-node Puppet architectures. It provides a
small HTTP API and an SSH loopback for r10k deployments, replacing heavier
Git hosting solutions.

---

## 🏗️ Architecture Overview

GitNano runs three layers on the same server:

- **Core (Bare Repo)** — /opt/gitnano/repos/control-repo.git (the source of truth)  
- **API (Flask / Gunicorn)** — listens on port **5000** for authenticated HTTP
  traffic  
- **Handshake (SSH)** — pe-puppet speaks to gitnano over localhost for r10k
  deployments

---

## 🛠️ Quick Operational Steps

### 1. Initialization
The installation script creates the `gitnano` system user, a Python virtualenv,
and migrates data from the previous GitLab instance.

Action:
```
systemctl status gitnano
```

### 2. Authentication
All API requests require a **Bearer Token** defined at installation. Include it
in requests:
```
Authorization: Bearer <YOUR_TOKEN>
```

### 3. Puppet / r10k Integration
r10k uses the private key at:
```
/etc/puppetlabs/puppetserver/ssh/id-control_repo.rsa
```
r10k connection string:
```
gitnano@<YOUR_FQDN>:/opt/gitnano/repos/control-repo.git
```

---

## 🌐 API Endpoints (Smart HTTP-compatible)

- `GET /<repo_name>/info/refs?service=git-receive-pack`  
  Purpose: advertise capabilities and refs (used during initial push/pull).  
  Header: `Authorization: Bearer <YOUR_TOKEN>`

- `POST /<repo_name>/git-receive-pack`  
  Purpose: receive pushed data into the repo (used on `git push`).  
  Header: `Authorization: Bearer <YOUR_TOKEN>`

---

## 💻 Usage Examples

Basic full-change lifecycle test:
```bash
cd /var/www/production
nano Puppetfile        # make a sample change
git add .
git commit -m "Puppetfile test change"
git push
puppet code deploy production --wait
cd /etc/puppetlabs/code-staging/environments/production
cat Puppetfile         # you should see your change
```

Pushing via the API (from live directory without SSH):
```bash
# curl check
curl -H "Authorization: Bearer YOUR_TOKEN" \
  "http://localhost:5000/control-repo.git/info/refs?service=git-receive-pack"

# git push (remote configured as 'gitnano')
git push gitnano production
```

Manual SSH handshake test (verify Puppet Master → Git backend):
```bash
sudo -u pe-puppet ssh -i /etc/puppetlabs/puppetserver/ssh/id-control_repo.rsa \
  gitnano@localhost "git --version"
```

---

## 📁 Key File Paths

| Component             | Path                                                        |
| :-------------------  | :---------------------------------------------------------- |
| **Bare Repository**    | /opt/gitnano/repos/control-repo.git                         |
| **Production Worktree**| /var/www/production                                         |
| **Python API Source**  | /opt/gitnano/app/gitnano.py                                 |
| **Puppet SSH Key**     | /etc/puppetlabs/puppetserver/ssh/id-control_repo.rsa        |
| **Systemd Service**    | /etc/systemd/system/gitnano.service                         |

---

## 🛡️ Troubleshooting

- **Permission Denied (SSH):** Ensure `/opt/gitnano` is owned by `gitnano` and
  `/opt` is owned by `root`. SSH will reject keys if parent directories are
  world-writable.

- **401 Unauthorized:** Verify the token in the `Authorization` header.

- **Hanging Connections:** Often caused by DNS lookups; the installer disables
  `UseDNS` in `sshd_config` to prevent this.

---

This setup is optimized for single-node performance by using a local API plus an
SSH loopback to avoid external network overhead.
